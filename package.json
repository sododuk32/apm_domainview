{
  "name": "apm-domainview",
  "version": "0.0.0",
  "description": "Feature Tree — store and view domain knowledge as a 3-level branch tree (CLI + optional MCP)",
  "type": "module",
  "bin": {
    "apm-domainview": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "pretest": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "feature-tree",
    "domain",
    "tree",
    "knowledge",
    "cli"
  ],
  "author": "sododuk32",
  "license": "UNLICENSED",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
