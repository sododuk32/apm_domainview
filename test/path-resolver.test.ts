import { describe, it } from "vitest";

describe("path-resolver", () => {
  it.todo("findNode 빈 경로 = 루트");
  it.todo("findNode 1 depth (브랜치)");
  it.todo("findNode 2 depth (기능)");
  it.todo("findNode 존재하지 않는 경로 → undefined");
  it.todo("findParent 루트의 부모 → undefined");
  it.todo("findParent 1 depth 의 부모 = 루트");
  it.todo("findParent 2 depth 의 부모 = 1 depth");
  it.todo("validatePath 없으면 throw");
});
