import { describe, it } from "vitest";

describe("storage", () => {
  it.todo("readDomain 정상 트리");
  it.todo("readDomain 존재하지 않음 → throw");
  it.todo("writeDomain 새 도메인 폴더 + index.json 생성");
  it.todo("writeDomain 기존 도메인 갱신 (덮어쓰기)");
  it.todo("listDomains 빈 워크스페이스 → []");
  it.todo("listDomains 다중 도메인 알파벳 정렬");
  it.todo("exists true / false");
});
