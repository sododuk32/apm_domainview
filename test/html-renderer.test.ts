import { describe, it } from "vitest";

describe("html-renderer", () => {
  it.todo("빈 트리 렌더 — 도메인 헤더만");
  it.todo("1 depth 트리 (도메인 + 브랜치) 렌더");
  it.todo("2 depth 트리 (도메인 + 브랜치 + 기능) 렌더");
  it.todo("description 헤더에 표시");
  it.todo("자체포함 — 출력에 외부 http(s)://, link rel=stylesheet, script src= 없음");
  it.todo("3색 태그 클래스(도메인/브랜치/기능) 존재");
  it.todo("접기/펼치기 토글 vanilla JS 내장");
});
